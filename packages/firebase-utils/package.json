{
	"name": "@quirk-a-bot/firebase-utils",
	"version": "1.0.0",
	"private": true,
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"description": "",
	"scripts": {
		"test": "yarn build && jest --colors --logHeapUsage --detectOpenHandles",
		"build": "rm -rf dist && yarn && npx tsc --build"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"firebase": "^8.2.10"
	},
	"devDependencies": {
		"@types/jest": "^26.0.20",
		"@types/node": "^14.14.22",
		"dotenv": "^8.2.0",
		"jest": "^26.6.3",
		"ts-jest": "^26.5.1",
		"typescript": "^4.1.5"
	}
}
