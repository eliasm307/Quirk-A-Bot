{
	"name": "@quirk-a-bot/data-model",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": "rm -rf dist && yarn build && npx tsc && jest --logHeapUsage",
		"start": "yarn build && node .",
		"dev": "yarn build && nodemon .",
		"build": "yarn && npx tsc"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@types/node": "^14.14.22",
		"fs-extra": "^9.1.0"
	},
	"devDependencies": {
		"@types/fs-extra": "^9.0.6",
		"@types/jest": "^26.0.20",
		"jest": "^26.6.3",
		"nodemon": "^2.0.7",
		"ts-jest": "^26.5.1",
		"typescript": "^4.1.5"
	}
}
