{
  "name": "@quirk-a-bot/data-model",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "nohoist": [
    "**/*jest*",
    "**/*jest*/**",
    "**/*rxjs*",
    "**/*rxjs*/**"
  ],
  "scripts": {
    "test": "npx tsc && yarn build && jest --colors --logHeapUsage",
    "testf": "npx tsc && yarn build && jest --colors --logHeapUsage --onlyFailures",
    "build": "rm -rf dist && yarn && npx tsc -p tsconfig.build.json",
    "firebase:start-emulators": "yarn --cwd ../../ start-emulators"
  },
  "author": "",
  "license": "ISC",

  "peerDependencies": {
    "typescript": ">= 4.3.0"
  },
  "dependencies": {
    "@quirk-a-bot/common": "*",
    "rxjs": "^7.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.10",
    "@babel/core": "^7.13.10",
    "@babel/plugin-proposal-class-properties": "^7.13.0",
    "@babel/preset-env": "^7.13.10",
    "@babel/preset-typescript": "^7.13.0",
    "@types/fs-extra": "^9.0.6",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.22",
    "babel-jest": "^27.0.2",
    "firebase": "^8.2.10",
    "fs-extra": "^9.1.0",
    "jest": "^27.0.4",
    "kill-port": "^1.6.1",
    "nodemon": "^2.0.7",
    "ts-jest": "^27.0.2",
    "url-exist-sync": "^1.0.2"
  }
}