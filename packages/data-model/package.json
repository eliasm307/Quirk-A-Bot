{
	"name": "@quirk-a-bot/data-model",
	"version": "1.0.0",
	"description": "",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "yarn && yarn build && node .",
		"dev": "yarn && nodemon .",
		"build": "npx tsc",
		"prepublish": "npx tsc"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@types/node": "^14.14.22",
		"fs-extra": "^9.1.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.7",
		"@types/fs-extra": "^9.0.6"
	}
}
