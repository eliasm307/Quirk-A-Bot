describe("UserViewModel", () => {
  it("doesn't load users that don't exist", () => {
    expect.hasAssertions();
  });

  it("can load a user that exists and edit the user", () => {
    expect.hasAssertions();
  });
});

export {};
