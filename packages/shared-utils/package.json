{
	"name": "@quirk-a-bot/shared-utils",
	"version": "1.0.0",
	"private": true,
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"description": "",
	"scripts": {
		"test": "rm -rf dist && yarn build && npx tsc && jest --colors --logHeapUsage",
		"build": "yarn && npx tsc --build"
	},
	"author": "",
	"license": "ISC",
  	"dependencies": {
		"@types/node": "^14.14.22",
		"firebase": "^8.2.10"
	},
	"devDependencies": { 
		"@types/jest": "^26.0.20",
		"jest": "^26.6.3", 
		"ts-jest": "^26.5.1",
		"typescript": "^4.1.5"
	}
}
