{
  "name": "@quirk-a-bot/common",
  "description": "Package with common items for quirk-a-bot",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "nohoist": [
    "**/*jest*",
    "**/*jest*/**"
  ],
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "yarn build && jest --colors --logHeapUsage",
    "build": "npx tsc && rm -rf dist && yarn && npx tsc --build tsconfig.build.json",
    "start-emulators": "firebase emulators:start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^8.2.10"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.34",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.1",
    "typescript": "^4.1.5",
    "url-exist-sync": "^1.0.2"
  }
}