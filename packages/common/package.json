{
  "name": "@quirk-a-bot/common",
  "description": "Package with common items for quirk-a-bot",
  "version": "1.0.0",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "nohoist": [
    "**/*jest*",
    "**/*jest*/**"
  ],
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "yarn && npx tsc && jest --colors --logHeapUsage",
    "build": "npx tsc --build tsconfig.build.json",
    "prebuild": "rm -rf dist && yarn && npx tsc",
    "start-emulators": "firebase emulators:start"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "typescript": ">= 4.3.0"
  },
  "dependencies": {
    "firebase": "^8.2.10",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.34",
    "@types/uuid": "^8.3.0",
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.1",
    "url-exist-sync": "^1.0.2"
  }
}
