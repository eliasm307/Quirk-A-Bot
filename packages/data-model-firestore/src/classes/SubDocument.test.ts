describe("SubDocument", () => {
  it("can write locally and to firestore", () => {
    expect.hasAssertions();
  });
  it("can read locally", () => {
    expect.hasAssertions();
  });
  it("can be initialised from existing data", () => {
    expect.hasAssertions();
  });
  it("can handle delete", () => {
    expect.hasAssertions();
  });
  it("can write locally only", () => {
    expect.hasAssertions();
  });
  it("can write to firestore", () => {
    expect.hasAssertions();
  });
  it("can write to firestore", () => {
    expect.hasAssertions();
  });
});
